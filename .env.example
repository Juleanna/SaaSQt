POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DJANGO_SECRET_KEY=dev-secret
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
SERVICES_SHARED_TOKEN=dev-service-token
SERVICES_JWT_SECRET=dev-service-hs256-secret
# Optional JWKS URL for RS256 verification in Orgs (not used with HS256):
# SERVICES_JWKS_URL=http://auth:8000/api/service/jwks
ROTATE_JWKS_EVERY_HOURS=12
# Or prefer exact hour UTC (0-23). If set, overrides ROTATE_JWKS_EVERY_HOURS
ROTATE_JWKS_CRON_HOUR=2
# Cursor pagination defaults
PAGE_SIZE=25
CURSOR_ORDERING=-id
